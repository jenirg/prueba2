version: "3"
services:
  db:
    image: "postgres:latest"
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=url.2024
      - POSTGRES_USER=url
      - POSTGRES_DB=consultorio2024